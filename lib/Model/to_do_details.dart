class ToDoDetails {
  int id;
  String title;
  String startDate;
  String endDate;
  String status;
  String timeLeft;
  bool isExpired;

  ToDoDetails({this.id, this.title, this.startDate, this.endDate, this.status, this.timeLeft, this.isExpired});
}